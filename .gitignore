# Learning infrastructure — never committed
course/
tools/
progress.json
.claude/

# Build artifacts
/server

# Python (legacy, no longer used)
__pycache__/
*.pyc
venv/
.venv/

# Node.js (legacy, no longer used)
node_modules/
package.json
package-lock.json

# Old scraped data
output.csv

